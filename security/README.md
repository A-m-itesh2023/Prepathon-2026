# Security Demonstration

The RCA agent uses a read-only, allow-listed tool gateway. Observability content is treated as untrusted input. Command execution, secret access, host access, and unapproved resources are outside the permitted capability boundary.
